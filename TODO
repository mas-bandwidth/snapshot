DONE

	Convert the bitpacker to C style

TODO

	Update the bitpacker tests to C style

	------

	Convert the library to compile as C, not C++

	------

	Implement a schema that fully defines the set of channels, messages, object types, tables attached to objects, table strategies, player cmds etc.

	In short, *everything* in the snapshot protocol will be driven by the schema, not by C++.

	------

	Make sure sockets are updated to support dual stack sockets

	------

	Make sure packet tagging is supported

	------
