DONE

TODO

	Implement a schema that fully defines the set of channels, messages, object types, tables, player cmds etc.

	In short, *everything* in the snapshot protocol will be driven by the schema.

	------

	Even, max packet sizes, MTU other things like this (!!!)

	------

	Make sure sockets are updated to support dual stack sockets

	------

	Make sure packet tagging is supported

	------